# API Server segment
# --- base deps (inlined; was shared/requirements-base.txt) ---
SQLAlchemy==2.0.49
asyncpg==0.31.0
pgvector==0.4.2
pydantic==2.13.4
pydantic[email]
pydantic-settings==2.14.1
inflect==7.5.0
python-dotenv==1.2.2
redis==5.3.1
arq==0.26.3
# Utils.Middlewares (DBProxy/performance_timer) subclasses FastAPI APIRouter,
# so fastapi is required by every segment (workers included).
fastapi==0.136.1

uvicorn==0.46.0
# C event loop + C HTTP parser for uvicorn (picked up by loop="auto"/http="auto" in app/main.py).
# Both are Linux-image optimisations: no Windows builds for every Python a dev machine may run, and
# uvicorn falls back to asyncio + h11 on its own when they are absent.
uvloop==0.21.0; sys_platform != "win32"
httptools==0.6.4; sys_platform != "win32"
starlette==1.0.0
python-multipart==0.0.29
orjson==3.11.9

# Excel/aircraft upload handling in request path (DBQueries/AircraftsExcel)
pandas==3.0.3
numpy==1.26.4
openpyxl==3.1.5

# DB migrations
alembic==1.18.4

# server-to-server forward to file-processor
httpx==0.28.1

# Power BI Embedded capacity control: ARM token acquisition for the dedicated control SPN.
# httpx (above) is the HTTP client; do NOT add azure-mgmt-powerbidedicated (heavy, blocking pollers).
# aiohttp is the async transport azure.identity.aio.ClientSecretCredential uses to fetch the ARM token
# (the .aio credential requires it; without it the client fails with "aiohttp package is not installed").
azure-identity==1.19.0
aiohttp==3.11.11
